# Perfil usado quando a aplicacao roda dentro do container Docker Compose,
# apontando para o servico "db" pelo nome (rede interna do compose).
spring.datasource.url=jdbc:mysql://db:3306/gestao-academico?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:root123}

jwt.secret=${JWT_SECRET:NuEDTlWWH/WitmbS7jdO2NiU0LICTGhNTY2rcqjq0adAzCS6kBRBgrkCmwXK+nReqh+Y9stgKEcHCnbZ3H213w==}
jwt.expiration=${JWT_EXPIRATION:86400000}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}
cors.allowed-methods=${CORS_ALLOWED_METHODS:GET,POST,PUT,DELETE,OPTIONS,PATCH}
cors.allowed-headers=${CORS_ALLOWED_HEADERS:Authorization,Cache-Control,Content-Type}

logging.level.gestao.academico=INFO
